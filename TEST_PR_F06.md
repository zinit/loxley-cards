# Test PR for F-06 verification

Plik testowy do zweryfikowania że workflow `.github/workflows/ai-review.yml`
triggeruje się na PR i bot (Claude Sonnet 4.6 przez Vercel AI SDK)
postuje komentarz z rubryką 5 kryteriów + werdyktem.

Po teście ten PR zostaje closed bez merge — plik nie ląduje na main.
