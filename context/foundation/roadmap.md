---
project: loxley-cards
version: 2
status: draft
created: 2026-05-25
updated: 2026-05-25
prd_version: 1
main_goal: speed
top_blocker: time
---

# Roadmap: loxley-cards

> Derived from `context/foundation/prd.md` (v1) + auto-researched codebase baseline.
> Edit-in-place; archive when superseded.
> Slices below are listed in dependency order. The "At a glance" table is the index.

## Vision recap

Gwint jest zablokowany wewnątrz Witchera 3 — nie istnieje lekka, webowa wersja tej mechaniki (rzędy, pogoda, best-of-3). Loxley-cards to standalone webowa karcianka inspirowana Gwintem: logowanie username + hasłem, partia vs bot (~15 min), kampania z progresją. Grupa docelowa: 1–10 osób, hobby-projekt budowany w 3 tygodnie wieczorami.

## North star

**S-03: Polished game UI** — gracz rozgrywa pełną partię best-of-3 vs prawdziwy bot w pięknym UI z S-01, podpiętym do REST API z S-02. Click-to-select kart, animacje, dynamiczne meta-tiles, end-of-round/match screens, lokalna persystencja unlocked stages. To walidacja głównej hipotezy produktu: czy silnik gry + UI grają razem jako produkt, nie jako prototyp.

> Gwiazda przewodnia — najmniejsza końcowa funkcjonalność, której dostarczenie udowadnia główną hipotezę produktu. Umieszczona w środku sekwencji (nie na początku), bo poprzedzające ją slice'y (S-01 statyczny UI z mock'iem, S-02 playable game API w prymitywnym debug UI) świadomie rozdzielają walidację wizualną od walidacji client-server — najpierw projektujemy UI z mock'iem, potem API z brzydkim frontem, S-03 łączy oba w spójną grę.

## At a glance

| ID   | Change ID                | Outcome (user can …)                                                              | Prerequisites      | PRD refs                          | Status   |
|------|--------------------------|-----------------------------------------------------------------------------------|--------------------|-----------------------------------|----------|
| F-01 | game-engine-core         | (foundation) silnik gry gotowy — reguły, scoring, bot, pełny zestaw abilities      | —                  | Business Logic, NFR (bot < 2s)    | done     |
| F-02 | data-persistence         | (foundation) warstwa persystencji — encje, migracje Flyway, połączenie z PostgreSQL | —                  | Guardrails, Access Control        | done     |
| F-03 | password-auth-scaffold   | (foundation) auth username + hasło — Spring Security, BCrypt, JWT w HTTPOnly cookie | F-02               | Access Control, NFR (prywatność)  | done     |
| F-04 | frontend-scaffold        | (foundation) Vite + React + TS scaffold; routing; jedna strona "loxley-cards"    | —                  | (architectural enabler dla S-01)  | done     |
| S-01 | static-ui-prototype      | przejść przez 2 ekrany gry (kampania + plansza) z animowanym przejściem; klikalne ale bez logiki gameplay; menu/login/score odłożone do osobnych slice'ów | F-04               | US-01, NFR (duży ekran)    | done     |
| S-02 | playable-game-api        | rozegrać pełną partię vs bot przez REST API (lokalnie, w prymitywnym debug UI) — backend wystawia engine z F-01 jako endpointy `POST /games` / `GET /games/{id}` / `POST /games/{id}/moves` z autoreply bota; frontend dostaje API client + thin debug page do walidacji kontraktu | F-01               | US-01, FR-004–FR-008 (warstwa API + walidacja kontraktu) | done     |
| S-03 | polished-game-ui         | rozegrać pełną partię vs bot w pięknym UI z S-01 — click-to-select kart, animacje, dynamiczne meta-tiles, end-of-round/match screens, lokalna persystencja unlocked stages w `localStorage` | S-01, S-02         | US-01, FR-004–FR-008, FR-009–FR-011 | done     |
| S-03b| polished-game-ui-improvements | dopolerować rozgrywkę — diagnoza horn doubling bug, popup notyfikacyjny ruchu (player + opponent), OPPONENT PASSED indicator, weather/horn empirical validation, round transition cleanup, ewentualnie emoji→SVG, match-end polish | S-03 | US-01, FR-004–FR-011 (polish na bazie S-03) | done     |
| S-04 | password-login           | zarejestrować się (username + hasło), zalogować się, wylogować                     | F-03               | US-02, FR-001, FR-012             | done     |
| S-05 | campaign-progression     | przeglądać kampanię, grać etapy, widzieć zapisany progres                          | F-02, S-03, S-04   | US-01, FR-002, FR-003, FR-009–FR-011 | proposed |

## Streams

Grupowanie elementów po łańcuchu zależności — pomoc nawigacyjna. Kanoniczne uporządkowanie to graf zależności w sekcjach Foundations i Slices poniżej.

| Stream | Theme                       | Chain                                              | Note                                                                          |
|--------|------------------------------|----------------------------------------------------|-------------------------------------------------------------------------------|
| A      | Frontend i rozgrywka         | `F-04` → `S-01` → `S-02` → `S-03` → `S-03b` → `S-05` | Gwiazda przewodnia jest w tym strumieniu (S-03); S-03b to follow-up polish do S-03 (parallel-safe z S-04/S-05); S-05 dołącza z B (wymaga S-04). |
| B      | Auth i persystencja          | `F-02` → `F-03` → `S-04`                          | Odblokowanie logowania; dołącza do A przy S-05.                              |
| C      | Silnik gry (atomic)          | `F-01`                                            | Samodzielny chunk pracy do równoległej egzekucji; dołącza do A przy S-02 (gdzie engine zostaje wystawiony przez REST). |

## Baseline

Stan codebase na 2026-05-25 (auto-researched + potwierdzone przez użytkownika). Foundations poniżej zakładają że te warstwy są na miejscu i NIE budują ich od zera.

- **Frontend:** absent — `frontend/` to pusty placeholder; brak frameworka, komponentów, routingu
- **Backend / API:** partial — Spring Boot 4.0.6 scaffold (multi-module Maven, `LoxleyCardsApplication.java`), zero kontrolerów i endpointów REST
- **Silnik gry:** absent — `acommon-game-engine/` to pusty stub; brak reguł, scoringu, bota, abilities
- **Data:** absent — `acommon-db/` pusty scaffold; brak encji JPA, repozytoriów, migracji Flyway, drivera PostgreSQL w POM
- **Auth:** absent — brak `spring-boot-starter-security`, brak implementacji password auth / BCrypt / JWT, brak konfiguracji security
- **Deploy / infra:** absent — brak Dockerfile, docker-compose, CI/CD workflows, nginx config (plan udokumentowany w `infrastructure.md`)
- **Observability:** absent — brak Actuator, logback, error tracking, metryk

## Foundations

### F-01: Silnik gry

- **Outcome:** (foundation) silnik gry gotowy — reguły partii (best-of-3, rzędy close/ranged/siege), layered scoring per karta z modifier orderingiem (hero immunity → weather → tight bond → morale boost → horn), trzy strategie bota (`RandomBot` + `HeuristicEasyBot` + `HeuristicMediumBot`) przypisywane per etap kampanii przez `BotStrategyResolver`, JSON-driven sample card / deck / campaign data (`src/main/resources/{cards,data}/*.json`) ładowane przez Spring beany na starcie, abilities z Witcher 3 Gwint: weather (3 warianty rzędów), commander's horn, spy, medic, tight bond, scorch, decoy, morale boost, clear weather, hero immunity, leader abilities (obecnie CLEAR_WEATHER). Atomowy — wszystkie abilities wchodzą razem. Plus sibling module `acommon-game-cli` z `LoxleyCliApplication` (`@SpringBootApplication`) jako standalone CLI runner: tryb default robi bot evaluation (4 matchups × 50 games), profile evaluation (5 opponent profiles × 30 games) i seeded bot-vs-bot simulation; tryb `@Profile("cli-player")` to interactive REPL (player vs bot przez stdin) — daje end-to-end smoke i ręczne testowanie engine'u bez czekania na REST (S-03) lub frontend (S-01/S-02/S-03).
- **Change ID:** game-engine-core
- **PRD refs:** Business Logic (pełny opis reguł i modyfikatorów), NFR (bot < 2s)
- **Unlocks:** S-03 (integracja partii z silnikiem)
- **Prerequisites:** —
- **Parallel with:** F-02, F-04, S-01, S-02
- **Blockers:** —
- **Unknowns:** —
- **Known gap vs PRD:** **muster** ability nie zaimplementowana (zero pokrycia w kodzie, brak `MusterEffect`, brak użycia w sample JSON). Pozostałe abilities z PRD są zaimplementowane; engine dodatkowo wnosi 3 extras (scorch, decoy, morale boost) — wszystkie autentyczne Witcher 3 Gwint, włączone bez extra kosztów. Muster może zostać dodany w follow-up change jeśli okaże się krytyczny dla docelowego Gwint feel.
- **JSON sample data caveat:** aktywnie ładowane JSONy (`data/sherwood_reference_ruleset.json`, `data/decks/sherwood_*_deck.json`, `data/campaign_stages.json`) zawierają named Witcher 3 characters (Foltest, Vernon Roche, Sigismund Dijkstra, John Natalis, Ves, Stennis, Blue Stripes Commando) — CDPR IP. Akceptowalne dla wewnętrznego MVP demo i edukacyjnego publicznego repo (10xDevs showcase); przed properly public release JSON content powinien być re-themed (Java code nie ma w sobie żadnego W3 IP).
- **Risk:** Najgęstszy moduł w projekcie — pełny zestaw abilities musi być spójny i przetestowany razem. Opóźnienie tu opóźnia gwiazdę przewodnią (S-03). Atomowość świadoma: abilities są wzajemnie zależne (scoring rzędu uwzględnia naraz pogodę, horn, tight bond, spy itd.), bot wymaga kompletu reguł żeby cokolwiek sensownie wybrać, izolowane testowanie 1 ability bez reszty = absurd. Dlatego NIE rozbijamy planowania ani implementacji per-ability — wchodzi razem.
- **Status:** done

### F-02: Warstwa persystencji

- **Outcome:** (foundation) warstwa persystencji gotowa — encje JPA (User, CampaignProgress), repozytoria Spring Data, migracje Flyway, driver PostgreSQL w POM, konfiguracja HikariCP (max-pool-size=10 per infrastructure.md).
- **Change ID:** data-persistence
- **PRD refs:** Guardrails (progres nie ginie — zero utraty danych między sesjami), Access Control (User entity jako podstawa identyfikacji — schemat z V1 zakładał `email` jako identyfikator, F-03 V3 migration zastąpi go przez `username` po pivocie 2026-06-13)
- **Unlocks:** F-03 (auth potrzebuje encji User), S-05 (kampania potrzebuje persystencji progresu)
- **Prerequisites:** —
- **Parallel with:** F-01, F-04, S-01, S-02
- **Blockers:** —
- **Unknowns:** —
- **Risk:** Ryzyko niskie; commodity warstwa. Jedyna pułapka: mismatch HikariCP pool vs Supabase pgbouncer — udokumentowane w infrastructure.md, mitygowane przez max-pool-size=10.
- **Status:** done

### F-03: Auth username + hasło

- **Outcome:** (foundation) auth password gotowy — Spring Security configuration (zabezpieczenie endpointów), BCrypt password hashing, JWT session token w HTTPOnly cookie. V3 Flyway migration na User entity: dodaj `username VARCHAR(255) NOT NULL UNIQUE` + `password_hash VARCHAR(255) NOT NULL`, zluzuj `email` (DROP NOT NULL — `email` staje się opcjonalnym polem na przyszłość; **unique constraint zostaje** bo Postgres pozwala na wiele wierszy z `NULL` pod `UNIQUE`, więc anonymous userzy współistnieją; per plan-review F1 Fix A 2026-06-13 unikamy `DROP CONSTRAINT users_email_key` które nie jest portable do H2 test profile). **Backfill note:** tabela `users` aktualnie pusta (F-02 just shipped, zero registered users) — `ADD COLUMN NOT NULL` bez DEFAULT bezpieczne; gdyby kiedyś istniały rekordy, V3 byłaby 2-step (ADD nullable → backfill → ALTER SET NOT NULL). REST endpointy: `POST /auth/register`, `POST /auth/login`, `POST /auth/logout`. SB4 autoconfig sanity-check: prawdopodobnie `spring-boot-security` artefakt potrzebny analogicznie do `spring-boot-flyway` z F-02 — sprawdzić PRZED kodowaniem.
- **Change ID:** password-auth-scaffold
- **PRD refs:** Access Control (username + hasło, BCrypt, JWT, brak password reset), NFR (zero zbieranych danych osobowych obowiązkowo, email opcjonalny nullable)
- **Unlocks:** S-04 (logowanie username + hasłem)
- **Prerequisites:** F-02 (encja User i repozytorium — V3 migration buduje na V1+V2)
- **Parallel with:** F-01, F-04, S-01, S-02
- **Blockers:** —
- **Unknowns:** —
- **Risk:** SB4 per-tech autoconfig split (lesson z F-02) — sprawdzić czy `spring-boot-security` artefakt jest potrzebny, zamiast diagnozować "Spring Security middleware nie działa" w runtime. Drugie: `JWT_SECRET` w `.env` musi być długi (32+ random bytes) i NIE może wylądować w gicie ani w GitHub Secrets debug logach (repo public). Trzecie: świadomy brak password reset — userek zapomni hasła = Daniel manual UPDATE BCrypt hash w `users` table (akceptowalne dla 5–10 znajomych, udokumentowane w `infrastructure.md` risk register).
- **Status:** done

### F-04: Frontend scaffold

- **Outcome:** (foundation) Vite + React + TS scaffold istnieje; struktura projektu (folders, tsconfig, vite.config); routing (React Router lub Wouter — decyzja w `/10x-plan`); jedna strona "loxley-cards" pod `/` jako sanity-check; `npm run dev` startuje, `npm run build` produkuje statyczny output. Brak gameplay, brak assetów, brak komunikacji z backendem.
- **Change ID:** frontend-scaffold
- **PRD refs:** (architectural enabler — brak bezpośredniej FR; uzasadnienie: S-01 wymaga działającego frontendu)
- **Unlocks:** S-01 (statyczny prototyp UI potrzebuje działającego scaffoldu)
- **Prerequisites:** —
- **Parallel with:** F-01, F-02, F-03
- **Blockers:** —
- **Unknowns:** —
- **Risk:** Niski; standardowy Vite scaffold dobrze udokumentowany. Wybór bibliotek (router, state management, drag&drop, styling) **odracza się do S-01/S-02** — tutaj tylko goły szkielet. Pułapka: scope creep ("przy okazji dodam Tailwind", "przy okazji dodam shadcn") — definicja done: pusty projekt z `npm run dev` i jedną stroną.
- **Status:** done

## Slices

### S-01: Statyczny prototyp UI

- **Outcome:** gracz otwiera grę w przeglądarce → CampaignMap (fullscreen bitmap Sherwood, fit-to-viewport, 10 stump waypoints z 3 visual states: active/locked/completed, Roman numerals I–X) → klik na active pieniek → animowany przelot do GameBoard (entrance: stump-in 600ms spring → content-in 250ms fade) → plansza Gwinta w układzie lustro (6 rzędów close/ranged/siege per strona, meta-strip symmetry z LEADER+score po lewej i hand+deck+grave po prawej dla obu stron, ręka 5 kart) na tle pieńka-blatu, z losowanymi kartami z 28-elementowego FINAL_DECK (Robin Hood theme: bohaterowie, jednostki, specjalne, leader). Klik "Back to map" → reverse animation → return. Świadome zawężenie scope z roadmap (5 ekranów → 2): menu, login, score-modal odłożone do osobnych slice'ów (S-04 dla login).
- **Change ID:** static-ui-prototype
- **PRD refs:** US-01 (ekran kampanii i ekran rozgrywki — to co user widzi), NFR (gra wymaga dużego ekranu — walidacja layoutu na desktop)
- **Prerequisites:** F-04 (scaffold)
- **Parallel with:** F-01, F-02, F-03
- **Blockers:** —
- **Unknowns:**
  - Skąd assety graficzne kart (rysować, generować, używać open assets)? — Zrealizowane: 28 prawdziwych obrazów Robin Hood theme (public-domain folklore) bundlowane w `finalDeck.ts`, mapa Sherwood + pieniek-blat + ikony rzędów bundlowane w `assets/`. Wszystkie skonwertowane do WebP (cwebp lossy q=78-90).
- **Risk:** **Świadome odstępstwo od vertical-first** — UI-first dla zarządzania ryzykiem wizualnym (frontend Gwinta to najmniej znany obszar dla solo dev) i czasowym (mniejsze wymierne kroki). Pułapka: scope creep w UI — łatwo dopolerowywać assety w nieskończoność. Definicja done: każdy ekran renderuje się i jest klikalny, bez wymogu finalnej oprawy graficznej (placeholdery OK).
- **Status:** done

### S-02: Playable game API (vertical slice)

- **Outcome:** backend wystawia 3 REST endpointy: `POST /games` (utwórz nową grę z deck'iem + zwróć initial GameState + legal moves), `GET /games/{id}` (pełen state + legal moves), `POST /games/{id}/moves` (waliduj + wykonaj ruch gracza + autoreply bota + zwróć new state + new legal moves). In-memory `GameSessionStore` (HashMap + UUID, ginie na restart — akceptowalne dla MVP). DTO serializacja `GameState` dla wire'a. Frontend dostaje API client (`fetch` wrappery w `src/api/`) i **prymitywny debug page** `/debug-game` — lista legalnych ruchów jako klikalne przyciski, JSON pretty-printed dump state, button "new game". Nie ruszamy istniejącego ładnego UI z S-01. Wszystko lokalnie (`localhost:8080` + `localhost:5173`), bez deploymentu, bez auth. **Cel: udowodnić że client-server game flow działa end-to-end + zaprojektować kontrakt API zanim podpinamy ładne UI w S-03.**
- **Change ID:** playable-game-api
- **PRD refs:** US-01 (rozgrywka partii vs bot — pełna integracja przez REST), FR-004 (zagranie karty — przez API), FR-005 (pas — przez API), FR-006 (leader — przez API), FR-007 (wybór rzędu dla horn — przez API), FR-008 (wynik rundy/partii — w odpowiedzi API), NFR (bot < 2s — synchroniczna odpowiedź)
- **Prerequisites:** F-01 (silnik — to on dostarcza `GameState`, generowanie legalnych ruchów, bot, scoring; engine-integration-guide.md jest właśnie dla tego slice'a)
- **Parallel with:** F-02, F-03, S-04
- **Blockers:** —
- **Unknowns:**
  - Kontrakt DTO — czy serializować `GameState` 1:1 (najprostsze, ale leak'uje internal model do wire'a), czy zaprojektować view-model dla frontu (bezpieczniejsze, więcej kodu)? — Owner: autor. Block: no (decyzja w `/10x-plan playable-game-api`).
  - Bot autoreply — w tym samym request (state response po ruchu gracza zawiera już state po ruchu bota) czy osobny request (dwa round-tripy)? Synchroniczna pętla w jednym requeście jest prostsza i pasuje do NFR bot < 2s; rozdzielenie ułatwia ewentualną animację "bot myśli" w S-03. — Owner: autor. Block: no.
  - Frontend `/debug-game` route — pod jakim URL'em żeby nie kolidowało z istniejącymi `/` (CampaignMap) i `/game/:stageId` (GameBoard)? — Owner: autor. Block: no.
- **Risk:** Najważniejsza decyzja architekturalna projektu (kontrakt API client-server) — jeśli kontrakt jest źle zaprojektowany, S-03 będzie wymagało refactoru. Mitygacja: debug page właśnie po to istnieje — iterujemy kontrakt z brzydkim UI taniej niż z pięknym. Drugi risk: in-memory store ginie na restart backendu — akceptowalne dla lokalnego MVP, persystencja w F-02 (osobny change). Trzeci risk: brak autoryzacji — każdy może POST na czyjąś grę po znajomości UUID; akceptowalne lokalnie, auth dochodzi w F-03/S-04.
- **Status:** done

### S-03: Polished game UI — NORTH STAR

- **Outcome:** istniejące piękne UI z S-01 dostaje życie — `CampaignMap` i `GameBoard.tsx` przepisane z mock state na `useGameState()` hook gadający z API z S-02 (`POST /games`, `GET /games/{id}`, `POST /games/{id}/moves`). Click-to-select karty w ręce → highlight legalnych rzędów (z `legalMoves` w state response) → klik rzędu → POST move → animacja karty lecącej w docelowe miejsce → re-render z nowym state (zawierającym już bot reply). Pas, Use leader, popup wyboru rzędu dla horn. Dynamiczne meta-tiles (score per row × 2 strony, total, weather, leader available/used, hand/deck/grave count, round indicator). End-of-round overlay + end-of-match screen + restart + Back to map. Lokalna persystencja unlocked stages w `localStorage` (per-user persistence przychodzi w S-05 z Supabase). Debug page z S-02 usuwamy. Bot reaguje < 2s (NFR walidacja empiryczna).
- **Change ID:** polished-game-ui
- **PRD refs:** US-01 (pełna rozgrywka partii vs bot — produkt, nie prototyp), FR-004 (zagranie karty), FR-005 (pas), FR-006 (leader), FR-007 (wybór rzędu), FR-008 (wynik), FR-009 (powrót do kampanii, progres lokalny), FR-010 (restart przegranego), FR-011 (replay ukończonych), NFR (bot < 2s — empiryczne potwierdzenie)
- **Prerequisites:** S-01 (piękne UI fundament), S-02 (API kontrakt)
- **Parallel with:** F-03, S-04
- **Blockers:** —
- **Unknowns:**
  - Czy animacja karty lecącej z ręki do rzędu musi czekać na response z API (pessimistic UI: klik → spinner → response → animacja) czy gramy optimistic UI (klik → animacja od razu → API w tle → rollback jeśli error)? — Owner: autor. Block: no (decyzja w `/10x-plan polished-game-ui`).
  - Walidacja czy `legalMoves` z API wystarczy do podświetlania legalnych celów, czy potrzeba dodatkowej logiki UI (np. shake animation na illegal click)? — Owner: autor. Block: no.
- **Risk:** Integracja UI ↔ API — najbardziej widoczny dla użytkownika moment. Mock state z S-01 (flat shape) ≠ DTO z S-02 (engine domain) — adapter w state hook konieczny. To jest gwiazda przewodnia: jeśli ta partia nie jest grywalna jako produkt, reszta roadmapy traci sens. Walidacja: zagrać 5–10 pełnych partii ręcznie, sprawdzić że abilities działają zgodnie z PRD §Business Logic, że UI jest responsywne (bot < 2s), że animacje są spójne.
- **Status:** done

### S-03b: Polished game UI improvements — follow-up do S-03

- **Outcome:** dopolerowanie rozgrywki na bazie S-03 (archived 2026-05-29 → `context/archive/2026-05-29-s-03-polished-game-ui/`). 7 issues zaobserwowanych w testach manualnych S-03, świadomie odłożonych do osobnego polish change zamiast bugfix sprintu w środku north-star slice'a. Top priority: **diagnoza horn doubling bug** (5+10+horn → 5 zniknęło zamiast podwojenia — backend bug czy frontend rendering bug?). Reszta: notyfikacja "[Player/Opponent] zagrał X / spasował" (3s toast po obu stronach — confirmation feedback + symetria UI, bez tego gracz nie wie czy bot zrobił niewidzialny ruch np. weather/decoy, czy spasował), statyczny "OPPONENT PASSED" indicator w MetaPanel, weryfikacja czy weather/horn visual indicators (❄/🎺, blue tint, golden glow, power color-coding) działają empirycznie (kod jest, nigdy nie zaobserwowano podczas testów — RNG), cleanup round transition (Phase 3 2s hotfix vs Phase 5 proper RoundOverlay), ewentualnie emoji→SVG icons (decyzja UX vs wood/Sherwood theme), match-end overlay polish (uwagi bez specyfikacji — wymaga playtest).
- **Change ID:** polished-game-ui-improvements
- **PRD refs:** US-01 (pełna rozgrywka — polish), FR-004 (zagranie karty — fix horn doubling), FR-005 (pas — visibility), FR-007 (wybór rzędu — visibility), FR-008 (wynik — polish overlays)
- **Prerequisites:** S-03 (baseline UI + reducer + cardImageMap fundament do dopolerowania)
- **Parallel with:** F-03, S-04, S-05 (frontend-only polish, nie rusza auth ani campaign progression backend)
- **Blockers:** —
- **Unknowns:**
  - Czy horn doubling bug to backend (scoring bug) czy frontend (rendering gubi kartę)? — Owner: autor. Block: no (decyzja w Phase 1 diagnozy w `/10x-plan polished-game-ui-improvements`).
  - Czy backend już zwraca `lastMove` / `opponentLastMove` w response do popupu notyfikacyjnego, czy trzeba dodać pole do DTO? — Owner: autor. Block: no (sprawdzić shape API response w Phase 1).
- **Risk:** Niski-średni. Horn bug może być w backendzie (wymagałby zmiany w `acommon-game-engine`, nie tylko frontend) — jeśli tak, scope się rozszerza. Reszta to czysty frontend polish. Trzymać dyscyplinę: nie rozszerzać o "deck builder przy okazji", nie dotykać S-04/S-05 scope'u.
- **Status:** done

### S-04: Logowanie username + hasłem

- **Outcome:** gracz otwiera URL, widzi formularz login / register, wpisuje username + hasło. Pierwsza wizyta = rejestracja (toggle "Zarejestruj się"), kolejne = login. Po sukcesie ląduje na ekranie kampanii ze swoim progresem (sesja w JWT cookie HTTPOnly). Może się wylogować (czysta cookie + redirect do ekranu login).
- **Change ID:** password-login
- **PRD refs:** US-02 (logowanie username + hasłem), FR-001 (rejestracja + login username + hasło), FR-012 (logout)
- **Prerequisites:** F-03 (auth scaffold)
- **Parallel with:** F-01, F-04, S-01, S-02, S-03
- **Blockers:** —
- **Unknowns:**
  - Walidacja siły hasła — czy wymuszamy min length / complexity, czy "cokolwiek wpisze user"? UX vs security tradeoff. Decyzja w `/10x-plan password-login`. Owner: autor. Block: no.
- **Risk:** CORS / cookie domain — w produkcji `loxley.cards` (front) ↔ `api.loxley.cards` (back), JWT cookie musi być scope'owany na `.loxley.cards` żeby SPA mogła go wysłać. Lokalnie obejście vite proxy `/api` → `:8080` (same-origin trick z S-02). Drugie: brute-force protection — bez rate limitingu publicznie wystawiony `/auth/login` jest podatny na credential stuffing. MVP: zaakceptowane (5–10 znajomych, repo niegłośne); jeśli urośnie → dorzucić rate limiter.
- **Status:** done

### S-05: Kampania i progresja

- **Outcome:** zalogowany gracz widzi ekran kampanii z 10 etapami (liniowe odblokowanie), może rozpocząć partię na odblokowanym etapie, wygrana odblokowuje następny etap, progres jest zapisany trwale (przeżywa restart przeglądarki i powrót następnego dnia), może zrestartować przegrany etap i ponownie grać ukończone etapy.
- **Change ID:** campaign-progression
- **PRD refs:** US-01 (progres zapisany trwale, następny etap odblokowany), FR-002 (przeglądanie kampanii — 10 etapów), FR-003 (rozpoczęcie partii na odblokowanym etapie), FR-009 (powrót do kampanii, progres zapisany), FR-010 (restart przegranego etapu), FR-011 (replay ukończonych etapów)
- **Prerequisites:** F-02 (persystencja), S-03 (grywalna partia), S-04 (logowanie)
- **Parallel with:** —
- **Blockers:** —
- **Unknowns:**
  - Predefiniowane talie per etap — kto je projektuje (ręcznie autor, czy generator z F-01)? — Owner: autor. Block: no (decyzja w `/10x-plan campaign-progression`).
- **Risk:** Najbardziej zintegrowany slice — łączy silnik, auth, persystencję, frontend kampanii. Zależy od poprawności S-03 i S-04. Skala (10 etapów) jest mała — to nie skala, tylko integracja jest ryzykiem.
- **Status:** proposed

## Backlog Handoff

| Roadmap ID | Change ID                | Suggested issue title                                       | Ready for `/10x-plan` | Notes                                       |
|------------|--------------------------|-------------------------------------------------------------|-----------------------|---------------------------------------------|
| F-01       | game-engine-core         | Implement game engine: rules, scoring, bot, abilities       | yes                   | Run `/10x-plan game-engine-core`            |
| F-02       | data-persistence         | Set up JPA entities, Flyway migrations, PostgreSQL          | done                  | Archived 2026-06-12                          |
| F-03       | password-auth-scaffold   | Implement username + password auth (Spring Security + BCrypt + JWT) | done                  | Archived 2026-06-13                          |
| F-04       | frontend-scaffold        | Bootstrap Vite + React + TS scaffold (empty project)        | yes                   | Run `/10x-plan frontend-scaffold`           |
| S-01       | static-ui-prototype      | Static UI prototype — all screens, clickable, no logic      | no                    | Needs F-04 done first                       |
| S-02       | playable-game-api        | Playable game API — REST endpoints + in-memory store + thin debug page | done                  | Archived 2026-05-29                          |
| S-03       | polished-game-ui         | Polished game UI (north star) — connect S-01 to S-02 API + click-to-select + animations + meta-tiles | done                  | Archived 2026-05-29                          |
| S-03b      | polished-game-ui-improvements | Polish pass — horn bug, player+opponent move toast, OPPONENT PASSED, weather/horn validation, round transition cleanup | done                  | Archived 2026-05-30                          |
| S-04       | password-login           | Username + password register/login/logout flow              | done                  | Archived 2026-06-14                          |
| S-05       | campaign-progression     | Campaign screen with 10 stages and progress persistence     | no                    | Needs F-02 + S-03 + S-04 done first         |

## Open Roadmap Questions

1. **Czy istnieje już webowa gra z mechaniką Gwinta?** — Owner: autor. Block: — (nie blokuje żadnego slice'a, ale wpływa na positioning produktu). Źródło: PRD §Open Questions.

## Parked

- **Multiplayer (PvP online)** — Why parked: PRD §Non-Goals. MVP to wyłącznie gra vs bot. (Konsekwencja techniczna: w MVP nie potrzeba websocketów — REST request-response wystarczy dla bota synchronicznego.)
- **Deck building** — Why parked: PRD §Non-Goals. Predefiniowane talie per etap kampanii.
- **Mobile / responsive** — Why parked: PRD §Non-Goals. Desktop-only; 6 rzędów + ręka + UI wymaga dużego ekranu.
- **Ranking / leaderboard** — Why parked: PRD §Non-Goals. Single-player kampania, bez social features.
- **Generowanie kart przez AI** — Why parked: PRD §Non-Goals. Talie ręcznie zaprojektowane.
- **Wybór dowódcy / frakcji** — Why parked: PRD §Non-Goals. MVP: 1 leader, 1 frakcja.
- **AI-coach (analiza ruchów po partii)** — Why parked: shape-notes §Forward: stretch-goals. Priorytet po MVP, nie blokuje.
- **Deploy produkcyjny (Hetzner + Cloudflare Pages + Supabase)** — Why parked: świadomie odłożone przy `main_goal: speed`. Lokalna grywalna partia (S-03) waliduje produkt, deploy waliduje infrastrukturę — to dwie różne sprawy. Pełen plan w `context/deployment/deploy-plan.md` gotowy do egzekucji kiedy będzie potrzebny publiczny URL (znajomi, 10xBuilder cert).

## Done

- **S-04: password login UI w `frontend/` + `GET /auth/me` w `backend/app/` — Phase 1: `AuthController.me()` endpoint czytający `SecurityContextHolder` principal i zwracający `AuthResponse(username)` lub 401. **Plan-review F1 Fix A:** explicit `instanceof AnonymousAuthenticationToken` check przed `isAuthenticated()` — bez tego `AnonymousAuthenticationFilter` (Spring Security domyślnie włączony) wstawiałby `AnonymousAuthenticationToken` z principal=`"anonymousUser"` i `isAuthenticated()=true` dla niezalogowanych userów, `/auth/me` zwracałoby 200 `{"username":"anonymousUser"}` zamiast 401 — efektywnie psując całą auth gate. 2 nowe AuthControllerTests (`me_withValidCookie_returns200WithUsername`, `me_withoutCookie_returns401`). Phase 2: Vite proxy `/auth` → `:8080` (dodane obok istniejącego `/api` z S-02), `authApi.ts` (4 wrappery: `register`/`login`/`logout`/`fetchCurrentUser`, ten ostatni zwraca `null` na 401 zamiast throw bo unauthenticated to oczekiwany stan na page load), `AuthUser` type w `types.ts`. parseError() celowo duplicatowany z `gameApi.ts` (plan-review F3 + impl-review F2 oba skipped per YAGNI — 7 linii duplikatu < premature shared util). Phase 3: `AuthContext` (user state + loading flag + login/register/logout actions, wywołuje `fetchCurrentUser()` na mount żeby restorować sesję po refresh), `ProtectedRoute` wrapper (redirect na `/login` gdy `!user`, blank screen podczas `loading`), router rewire w `main.tsx` z `AuthProvider` wrapping `RouterProvider`. **Impl-review F1:** `useMemo` na context value object żeby uniknąć unnecessary re-renderów u wszystkich `useAuth()` consumerów (login/register/logout są `useCallback`'ami, ale wrapping object był recreated co render). Phase 4: `LoginPage.tsx` z single page + toggle (login/register w jednym komponencie, prostsze niż 2 routes per Q&A decision dla 5-10 użytkowników), client-side validation tylko non-empty check (per Q&A: "frontend ma być głupi, bezstanowy" — server jest source of truth dla regex + length rules), server error messages mapowane na ludzkie komunikaty (`USERNAME_TAKEN`/`BAD_CREDENTIALS`/validation errors), edge case "user już zalogowany na `/login`" → `<Navigate to="/" replace />`. Phase 5: CampaignMap auth chrome (logout button + username display) jako absolute-positioned pill w top-right rogu, semi-transparent dark wood background (`rgba(20,12,4,0.78)`) + gold border + backdrop blur + inset highlight (post-polish — pierwsza wersja miała tylko text-shadow który ginął na sunbeam areas mapy). **Polish phase (interactive z Daniel, bez formalnego agenta):** wygenerowany `landing-hero.webp` w Magnific Flux.2 Pro (Sherwood map jako Style reference + custom prompt "deep forest interior, low angle, vertical tree trunks, central corridor" — pierwsza iteracja kopiowała kompozycję mapy 1:1 mimo style transfer intent, druga iteracja z poprawkami "no Gwent in prompt" + "vertical composition" + "no path no pond" dała czyste corridor scenes; #4 z dramatic lighting wybrane). 2688×1536 PNG (7.3MB) → cwebp q=82 → 640KB webp. LoginPage rebuild: hero image jako absolute background (cover), dark gradient overlay (top → bottom: 0.15 → 0.75 opacity), title "LOXLEY CARDS" w Cinzel `clamp(36px, 6.4vw, 76px)` z 4-warstwowym text-shadow (gold aura + 3× czarny dla kontrastu na sunbeams), tagline "A tactical card game inspired by Gwent." italic, description 3-linijkowy mini-opis ("A single-player card game set deep in Sherwood Forest..."), form-card semi-transparent dark wood + gold border + backdrop blur (lżejszy: max-width 380px, padding 28/32/22 — pierwsza wersja 420px/36px wyglądała za ciężko), footer credit "A final project for the 10xDevs 3.0 course." absolute bottom-center. CampaignMap chrome polish: pill background + gold border + `user-select: none` + `cursor: default` na username, większe rozmiary (font 16/15px, padding 10×22, gap 16px) per Daniel feedback. 254 testów green w reactorze. Empiryczne testy: register marian/sherwood2 → 200 + redirect na `/` z CampaignMap + username "marian" w top-right + logout → `/login`; login robin/sherwood1 → 200 + CampaignMap; refresh logged-in → stays na `/` bez flash login page'a (auth context restoruje przez `/auth/me`); incognito direct URL `/game/1` → redirect na `/login`; duplicate register → "Username already taken"; bad password → "Bad credentials"; empty form → "Please fill in all fields".** — Archived 2026-06-14 → `context/archive/2026-06-14-s-04-password-login/`. Lesson: **interactive polish phase między formalnym `/10x-implement` a `/10x-impl-review` działa świetnie dla wizualnych nuance'ów** — Daniel + Claude w głównej sesji iterowali style (text shadows, opacity, padding, hero image generation, layout balance) w minutach przez Vite hot-reload zamiast spawnować implementer agent na każdą drobną CSS poprawkę; review widzi finalny stan (nie "what implementer built then human tweaked"), single commit obejmuje całość. Plus: **style reference w generatorach jak Magnific Flux.2 może być za silny** — pierwsza próba "Sherwood map jako style reference + new prompt" kopiowała composition mapy mimo intent style transfer; rozwiązanie: explicit negative prompt (`no path, no pond, no clearing`) + wymuszenie innej perspektywy (`vertical composition, low angle looking up`) + reduce reference influence ALBO wyrzucenie reference image dla tej generacji. Plus: **prompt should avoid brand names** (`Witcher`, `Gwent`) jeśli model je rozpoznaje — Flux.2 wyrzucił fake "WITCHER 2 Calrer 2017 Pihent & Beestar!" logo ghost overlay przy pierwszej próbie, mimo "no text" w negative prompt; przeformułowanie na `Eastern European fantasy painterly style` (descriptive) załatwiło sprawę. Plus: **Spring Security AnonymousAuthenticationFilter jest pułapką dla `/me` endpointów** — domyślnie włączony, wstawia `AnonymousAuthenticationToken` z `isAuthenticated()=true` co łamie naiwny "jeśli auth != null && isAuthenticated() → 200" pattern; `instanceof AnonymousAuthenticationToken` check jest standardową obroną (zamiast globalnego wyłączania anonymous auth które może wpływać na inne endpointy). Plus: implementer commit ritual disable pattern z F-03 zadziałał ponownie — explicit NOTE w Phase 1 prompt wyłączył built-in commit ritual na cały slice (5 phases), zero "ready for commit?" prompts w całej sesji. Pattern potwierdzony w 2 niezależnych slice'ach (F-03 + S-04), idzie do `feedback_implementer_commit_ritual` memory jako stable rule.** — Archived 2026-06-14 → `context/archive/2026-06-14-s-04-password-login/`.
- **F-03: password auth scaffold w `backend/app/` — V3 Flyway migration (`V3__add_username_password_auth.sql`): `ADD COLUMN username VARCHAR(255) NOT NULL UNIQUE` + `ADD COLUMN password_hash VARCHAR(255) NOT NULL` + `ALTER COLUMN email DROP NOT NULL` (unique index na email zostaje — plan-review F1 Fix A: Postgres NULL × N pod UNIQUE = distinct, więc anonymous userzy bez emaila współistnieją; pominięcie `DROP CONSTRAINT users_email_key` bo Postgres-specific name, H2 auto-gen różne nazwy → portability). User entity + UserRepository.findByUsername. POM: `spring-boot-starter-security` + `spring-boot-security` (SB4 autoconfig — sprawdzone WebSearchem przed kodowaniem per F-02 lesson, znaleziony w Maven Central, dodany od razu, zero runtime diagnostyki) + `jjwt-api`/`jjwt-impl`/`jjwt-jackson` 0.13.0. SecurityConfig (filter chain): `/auth/**` + `/api/games/**` permitAll (per Q3 — auth enforcement deferred do S-04/S-05), reszta `authenticated()`, CORS migrated z WebConfig do Spring Security, BCryptPasswordEncoder bean, JwtAuthenticationFilter w chain, **custom UserDetailsService bean** (post-review fix — loaduje user przez `UserRepository.findByUsername`, wraps w `org.springframework.security.core.userdetails.User`; bez tego beana Spring Security autoconfig logował `Using generated security password: <uuid>` przy każdym startupie i wstrzykiwał default in-memory user `user`, niespójność z resztą auth). JwtTokenProvider: generate (subject=username, iat, exp = iat + 604800s = 7 dni per Q2), validate; **constructor sprawdza key length po base64 decode, < 32 bytes → IllegalStateException** (impl-review F2). AuthController: `POST /auth/register` (BCrypt encode + save + JWT cookie + 201), `POST /auth/login` (UserRepository.findByUsername + passwordEncoder.matches + JWT cookie + 200), `POST /auth/logout` (clear cookie z Max-Age=0 + Expires=epoch). AuthDtos: RegisterRequest (`username` @Pattern `^[a-zA-Z][a-zA-Z0-9_-]{2,29}$` + `password` @Size(min=8) per Q4+Q5), LoginRequest, AuthResponse (`{username}` only — zero password/hash leak w response). GlobalExceptionHandler: 409 USERNAME_TAKEN (UsernameAlreadyExistsException) + 401 BAD_CREDENTIALS (Spring's BadCredentialsException) + 400 BAD_REQUEST (MethodArgumentNotValidException, czytelne validation messages) + **409 USERNAME_TAKEN dla DataIntegrityViolationException** (impl-review F1 — race fix gdy dwa concurrent register dla tego samego username przejdą findByUsername check przed save → drugi save'em DB UNIQUE constraint by inaczej rzucił 500). Cookie attributes: HttpOnly, SameSite=Lax, Path=/, Max-Age=604800, **Secure=profile-aware** (post-review F2 — `@Value("${app.cookie.secure:false}")` w AuthController, `app.cookie.secure=false` w application.properties + override `=true` w nowym `application-prod.properties` żeby prod HTTPS dostał Secure flag, dev HTTP nadal bez). 8 nowych AuthControllerTests z `@Transactional` dla cleanup (register success/duplicate/short-username/short-password, login success/bad-password/nonexistent-user, logout — per plan-review F3 wymaga real schema, więc test profile zaktualizowane na `spring.flyway.enabled=true` + `ddl-auto=validate`). GameControllerTests dostały `@AutoConfigureMockMvc(addFilters=false)` żeby bypassować security filter chain (per Q6 — game tests są auth-ortogonalne, brak boilerplate JWT w 8 istniejących testach). Empiryczna weryfikacja 7 curl scenariuszy: register robin/sherwood1 → 201 + Set-Cookie HttpOnly/SameSite=Lax/Max-Age=604800; login → 200 + fresh JWT; logout → 200 + Set-Cookie Max-Age=0/Expires=1970; duplicate register → 409 USERNAME_TAKEN; bad password → 401 BAD_CREDENTIALS; short username "ab" → 400 z czytelnym message; short password "short" → 400. JWT payload decoded: `{"sub":"robin","iat":...,"exp":iat+604800}` — exp - iat = 7 dni zgodnie z Q2. 252 testów green w reactorze, BUILD SUCCESS. **Mid-flow security incident i jego fix:** Hibernate 7 w SB4 zalogował JDBC URL z embedded password (`?user=...&password=<REDACTED>&sslmode=require`) **plaintext na INFO level** przy spring-boot:run startup (Flyway w tym samym logu maskuje password jako `********`, Hibernate nie). Daniel skopiował log do Claude session, hasło w plaintext w transcripcie (plus repo public). Plus accidentalny `Read .env` ujawnił JWT_SECRET (też w sesji). Mitygacja: (1) refactor configu — `DATABASE_URL` splitnięte na 3 osobne env vars (`DATABASE_URL` bez query credentials + `DATABASE_USERNAME` + `DATABASE_PASSWORD`), Hikari domyślnie maskuje password jako `********` gdy jest osobnym property zamiast embedded w URL query string; (2) `logging.level.org.hibernate.orm.connections.pooling=WARN` w main `application.properties` jako defense in depth, wycina całą "Database info: ..." sekcję ze startup logu (pokazywała też project ref + driver info — PII-ish dla public repo); (3) JWT_SECRET zrotowany (nowy `openssl rand -base64 48`), DB password Daniel zrotuje w Supabase dashboard ręcznie; (4) test profile properties dostały `spring.datasource.username=sa` + `spring.datasource.password=` żeby placeholders z main properties się resolwowały (bez tego H2 testy by wybuchły na `${DATABASE_USERNAME}`). **Docs cleanup PRZED F-03 implementation** (pivot magic-link → username/password decision na początku sesji): 7 plików live (`PRD`, `shape-notes`, `tech-stack`, `infrastructure`, `AGENTS.md`, `roadmap`, `deploy-plan`) + 4 memory files + rename change-id `f-03-magic-link-auth-scaffold` → `f-03-password-auth-scaffold` i `s-04-magic-link-login` → `s-04-password-login`. Powód pivota: Daniel ma Resend account ale pod inną domeną na free planie (free plan = 1 verified domain), verified `loxley.cards` przez SPF/DKIM/DMARC byłaby blockerem do F-03. Tradeoff zaakceptowany: brak password reset (manual UPDATE BCrypt hash w DB jak ktoś zapomni hasła; akceptowalne dla 5–10 znajomych, udokumentowane w `infrastructure.md` risk register), email opcjonalny nullable jako "future use" pole. **Docs post-correction** (po implementacji, ale przed archive): 4 miejsca w live docs (`PRD` NFR, `AGENTS.md` hard rule, `roadmap` F-03 outcome, `shape-notes` NFR) poprawione z błędnego "email nullable bez unique constraintu" na "email nullable, unique zostaje" — bo plan-review F1 Fix A zostawił unique index (Postgres NULL × N pod UNIQUE = distinct, więc anonymous userzy współistnieją). Per F-01/S-01 lesson "docs must match shipped reality before archive".** — Archived 2026-06-13 → `context/archive/2026-06-13-f-03-password-auth-scaffold/`. Lesson: **Hibernate 7 w SB4 loguje JDBC URL z embedded password na INFO level** — Flyway maskuje, Hibernate nie. ZAWSZE split credentials w Spring properties (`spring.datasource.username` + `spring.datasource.password` jako osobne properties) zamiast embedded w URL query string; dodaj `logging.level.org.hibernate.orm.connections.pooling=WARN` jako defense in depth. Plus: **Postgres NULL × N pod UNIQUE = distinct** — `email nullable + unique` dla "opcjonalne ale unikalne when set" działa razem; nie próbuj DROP UNIQUE jeśli i tak chcesz unikalność dla podanych wartości (plus DROP CONSTRAINT z Postgres-specific name jest non-portable do H2 test profile). Plus: **SB4 per-tech autoconfig sanity check pattern działa** (F-02 lesson reapplied) — agent zrobił WebSearch dla `spring-boot-security` PRZED kodowaniem (per explicit flag w F-03 risk), znalazł że artefakt JEST potrzebny i DOSTĘPNY w Maven Central, dodał od razu, zero runtime diagnostyki. Pattern "sanity-check SB4 autoconfig per technology przed kodowaniem" jest teraz potwierdzony w 2 niezależnych slice'ach (F-02 reactive discovery + F-03 proactive prevention). Plus: `/10x-impl-review` znajduje issues których plan-review pominął (F1 race condition, F2 JWT key length), ALE reviewer **pominął 2 issues** które ja wyłapałem manualnie (UserDetailsService bean missing — "Using generated security password" w logu; Cookie Secure flag dla prod profile) — manual sweep głównej sesji przed archive zostaje standardem (S-03 pattern reinforced, F-02 też używał tego). Plus: **implementer NIE proponował commit po fazie** (vs F-02/S-02/S-03/S-03b gdzie próbował 3-4 razy mimo memory rule "Daniel zarządza gitem") — explicit NOTE w prompcie `/10x-implement` ("Do NOT propose commit / git add steps after this or any subsequent phase") wyłączyło built-in commit ritual skilla na cały slice. Pattern do reapply w przyszłych slice'ach.
- **F-02: warstwa persystencji w `acommon-db` — encja `User` (UUID PK via JPA `@GeneratedValue(strategy=UUID)` + email VARCHAR UNIQUE + highest_unlocked_stage INT NOT NULL DEFAULT 1 + created_at/updated_at `Instant` via @PrePersist/@PreUpdate), `UserRepository` Spring Data JPA, 2 Flyway migracje (V1__create_users_table + V2__timestamps_to_timestamptz — impl-review F1 fix dla Instant↔TIMESTAMPTZ mapping). POM dependencies: `spring-boot-starter-data-jpa`, `postgresql` driver, `flyway-core` + `flyway-database-postgresql` + **`spring-boot-flyway`** (SB4 per-tech autoconfig split discovery — bez tego artefaktu Flyway w ogóle nie startuje, mimo że flyway-core jest na classpath; runtime diagnoza w Phase 2). HikariCP `max-pool-size=10` (5 connection w reserve dla Supabase Free pgbouncer limit 15). **Pgbouncer transaction-mode gotcha:** `spring.datasource.hikari.data-source-properties.prepareThreshold=0` w application.properties — bez tego "prepared statement S_X already exists" errors przy connection reuse (runtime diagnoza w Phase 3). Test profile H2 in-memory w obu modułach (`acommon-db/src/test/resources/application-test.properties` + `app/src/test/resources/application.properties` — F1 critical plan-review fix dla istniejących 8 testów w app module: LoxleyCardsApplicationTests + GameControllerTests, które bez test resources by wybuchły na resolvowaniu `${DATABASE_URL}`). Test starter: `spring-boot-starter-data-jpa-test` (SB4 per-tech split, plan był SB3 assumptions — implementer poprawnie zaadaptował). 5 UserRepositoryTests (save+UUID generation, findByEmail, findByEmail empty, updateStage, uniqueConstraint), 243+ tests green w całym reactorze. `backend/.env` (gitignored) + `backend/.env.example` (committed template z placeholderami i instrukcjami SKĄD wziąć — JDBC tab Transaction pooler w Supabase dashboard, port 6543). JDBC format requirement (nie URI z Supabase dashboard URI tab — Spring nie auto-konwertuje) udokumentowany w application.properties komentarzem i w .env.example. **Decyzja Daniela:** olać Docker compose dla lokalnego Postgresa — jedna konfiguracja Supabase dla dev+serv (po skończonym S-04, S-05 Daniel przepnie serv na fresh production Supabase project). Tech-stack.md zaktualizowany w sekcji "Spring side" (SB4 autoconfig split + pgbouncer prepareThreshold lessons) i "Local dev" (decyzja "jedna konfiguracja Supabase").** — Archived 2026-06-12 → `context/archive/2026-06-12-f-02-data-persistence/`. Lesson: **Spring Boot 4 rozdzielił autoconfig per technology** — biblioteki (`flyway-core`, `hibernate-core`, `postgresql` driver) to NIE WYSTARCZA, dla każdej trzeba osobny `spring-boot-X` autoconfig artifact (`spring-boot-flyway`, `spring-boot-jpa`, `spring-boot-hibernate`, etc.). Symptom: brak ślady "Flyway Community Edition" w logu startup mimo flyway-core w classpath. Lesson reapplied rekurencyjnie w Phase 3 do `spring-boot-starter-data-jpa-test`. Flag dla F-03: jeśli używamy Spring Security, sprawdzić czy `spring-boot-security` jest potrzebny analogicznie. **Plus: pgbouncer transaction-mode wymaga `prepareThreshold=0`** — fundamentalna decyzja platformowa, nieoczywista (Spring Boot + JDBC default ma server-side prepared statements ON), runtime errors są misleading ("prepared statement S_X already exists" — nie wskazuje na pgbouncer). Plus: `/10x-plan-review` znowu critical — F1 (app module tests by były złamane bez test resources) zatrzymał reactor build PRZED kodowaniem; reviewer poszedł do app module i wykrył że LoxleyCardsApplicationTests + GameControllerTests używają @SpringBootTest bez test profile → DATABASE_URL by się nie zresolwowało → 8 testów fail. `/10x-impl-review` złapał F1 TIMESTAMP↔TIMESTAMPTZ mismatch (Instant w Javie jest UTC, postgres TIMESTAMP bez zone = ambiguity) — trywialny V2 ALTER fix przed shipowaniem. Plus: implementer ten raz POSŁUCHAŁ memory rule "Daniel zarządza gitem + single commit per change" — zero prób "commit step after phase" w całym F-02 (vs S-02, S-03, S-03b gdzie były 3+ flagi).
- **S-03b: polish pass na bazie S-03 (north star) — 8 issues zaobserwowanych w testach manualnych S-03 + dorzucony mid-flow popup notyfikacyjny + 2 drobiazgi (toast x2 duration, PASSED indicator kolor). Phase 1: diagnoza horn bug = NO BUG (hipoteza "5 zniknęło to bot scorch po horn doubling" potwierdzona deterministycznie z dev endpoint forced-hand w Phase 4); round resolution backend działa poprawnie (Network proof: 1-0 → pass round 2 → 1-1 → round 3 starts normalnie), bug dotyczył tylko `MatchEndScreen.tsx` pokazującego DEFEAT dla draw matchWinner=null (fixed, DRAW state). Phase 2: `LastMoveView` DTO z dwoma polami `yourLastMove` + `opponentLastMove` (F1 critical plan-review fix — single field nie obsługiwał symmetric toast bo bot autoreply override player move), `rowKind` (CLOSE/RANGED/SIEGE) included from start (avoids follow-up DTO change), `DevTestController` pod `@Profile("dev")` z `/api/dev/games/forced-hand` (forced hand: horn + 3× weather cards, blocked w produkcji — 404). Phase 3: `MoveToast` component (3s→6s duration mid-flow expand), symmetric player+opponent, suppressed during round/match overlays, lastMove cleared on ROUND_DISMISSED + RESTART_GAME (F4 plan-review fix); OPPONENT PASSED indicator w `MetaPanel` (po fix kolor cream + Cinzel + black outline jak ROW labels — był słabo widoczny w orange); plus backend createGame fix (`Move lastBotMove = driveBotMoves(state, gameId)` zamiast ignorowanego return value — bot starter ~50% rozdań nie pokazywał toast'a). Phase 4: dev endpoint validation + `?gameId=` URL param w GameBoard (getGameState zamiast createGame gdy dev game w URL); weather (blue tint + ❄ + RED power numbers non-hero, hero immunity cream) + horn (orange + 🎺 + GREEN doubled power) + clear weather wszystkie wizualnie potwierdzone. Phase 5: match-end overlay polish skipowane (no concrete issues found po playtest). Impl-review fixes: F1 `useRef` pattern w MoveToast + RoundOverlay (timer reset on every render bug — partial first pass, RoundOverlay missed mimo flagi, fixed w second pass), F2 `roundDraw` rename (variable shadowing w MatchEndScreen.map()), F3 `findCardName` delegates do `findCardInstance` (DRY, -10 linii w GameStateMapper), F4 `gameIdRef.current = null` po RESTART_GAME (defense-in-depth). Skip: F5 DevTestController synchronized block (dev-only, no race risk, scope creep). Layout responsiveness przy bardzo niskich viewportach (board-row min-height 123px × 6 = 738px za dużo dla wide-short ekranów, hand nakłada się na board) — decyzja Daniela: "olać", workaround "graj na typowym viewport" (~1080px+), proper fix deferred do kolejnego polish slice'a. Plus mid-flow pre-commit addition: desktop-only guard (PRD §Non-Goals gap — do tej pory zero ochrony przed mobile user'em, layout się rozsypuje) — CSS-only fix w `index.html` + `index.css`: warning div + `@media (max-width: 1023px)` ukrywa `#root` i pokazuje pełnoekranowy komunikat "designed for desktop only" (Sherwood theme: Cinzel + cream + gold border + dark wood background), zero JS, automatic resize handling.** — Archived 2026-05-30 → `context/archive/2026-05-30-s-03b-polished-game-ui-improvements/`. Lesson: **diagnose-first** w Phase 1 dla "podejrzanych gameplay bugów" oszczędziło realnej roboty — horn bug okazał się false positive (bot scorch destroyed highest-strength unit po horn doubling, gracz źle zinterpretował), zero kodu zmienione, hipoteza potwierdzona przez dev endpoint deterministic test. Plus: implementer wciąż próbował proponować commit po fazie (trzeci raz w tym slice'em mimo trzykrotnie powtórzonej memory rule "Daniel zarządza gitem + single commit per change") — wzorzec do flagowania ostrzej w przyszłych slice'ach, skill ma built-in commit ritual który trzeba wyłączać explicit. Plus: `/10x-plan-review` znowu critical (F1: single lastMove nie satisfies symmetric toast requirement bo bot autoreply override player move — z kodu plan wyglądał OK, ale reviewer poszedł do controller code i znalazł że oba Move objects są dostępne, więc dwa pola są tanio osiągalne). Plus: `/10x-impl-review` znowu znalazł realne issues w wymaganiu trust-but-verify — F1 dotykało dwóch komponentów (MoveToast + RoundOverlay), implementer pierwszy raz fixnął tylko jeden mimo explicit flagi → druga runda potrzebna. Sanity check plików po impl-review triage przed manual archive zostaje standardem (S-03 lesson reinforced).
- **F-04: (foundation) Vite + React + TS scaffold istnieje; struktura projektu (folders, tsconfig, vite.config); routing (React Router lub Wouter — decyzja w `/10x-plan`); jedna strona "loxley-cards" pod `/` jako sanity-check; `npm run dev` startuje, `npm run build` produkuje statyczny output. Brak gameplay, brak assetów, brak komunikacji z backendem.** — Archived 2026-05-25 → `context/archive/2026-05-25-f-04-frontend-scaffold/`. Lesson: —.
- **F-01: (foundation) silnik gry gotowy w `backend/acommon-game-engine/` — reguły partii (best-of-3, rzędy close/ranged/siege), layered scoring per karta z modifier orderingiem (hero immunity → weather → tight bond → morale boost → horn), trzy strategie bota (`RandomBot`, `HeuristicEasyBot`, `HeuristicMediumBot`) z `BotStrategyResolver`, JSON-driven sample card/deck/campaign data, abilities z Witcher 3 Gwint (weather × 3, commander's horn, spy, medic, tight bond, scorch, decoy, morale boost, clear weather, hero immunity, leader). 200 testów. Plus nowy sibling module `acommon-game-cli` (`LoxleyCliApplication`) — standalone CLI runner z bot evaluation, profile evaluation, bot-vs-bot simulation i interactive `cli-player` REPL (29 testów). Reactor: 6 modules, BUILD SUCCESS. Plus dev-facing `context/foundation/engine-integration-guide.md` — przewodnik dla S-03 (REST controllers + WebSocket): public API surface, sample REST controller (~70 linii compilujący się), Spring config preparation, DTO design proposal, edge cases. **Known gap vs PRD:** muster ability nie zaimplementowana — deferred jako follow-up change, jeśli okaże się krytyczna dla docelowego Gwint feel.** — Archived 2026-05-26 → `context/archive/2026-05-26-f-01-game-engine-core/`. Lesson: docs muszą być zsynchronizowane z faktycznym kodem (jeśli implementacja świadomie idzie poza ramy oryginalnego planu, zaktualizuj plan / brief / reviews / PRD / roadmap zanim zarchiwizujesz — archive jest read-only by convention).
- **S-01: gracz otwiera grę w przeglądarce → CampaignMap (fullscreen bitmap Sherwood, fit-to-viewport, 10 stump waypoints z 3 visual states + Roman numerals) → klik na active pieniek → animowany przelot do GameBoard (entrance: stump-in 600ms spring → content-in 250ms fade @ t=700ms, reverse na exit) → plansza Gwinta lustro (6 rzędów close/ranged/siege per strona, meta-strip symmetry left/right, ręka 5 kart) na tle pieńka-blatu z losowanymi kartami z 28-elementowego FINAL_DECK (Robin Hood theme — bohaterowie, jednostki, specjalne, leader, runtime randomization 15 unique per page-load). WebP asset pipeline (cwebp lossy q=78-90): główne assety 23MB → 4.6MB, karty 75MB → 3.9MB. Plus iteracje visual polish: animation sequencing, meta-strip symmetry refactor, row height tuning, label visibility z text-shadow outline, power circle font (Cinzel → system sans + lining-nums), row icon glow (4-warstwowy filter), back button miniaturization. Świadome zawężenie scope z roadmap S-01 spec (5 ekranów → 2): menu, login, score-modal odłożone do osobnych slice'ów. Plan + brief + reviews zsynchronizowane do shipped reality PRZED archive.** — Archived 2026-05-28 → `context/archive/2026-05-28-s-01-static-ui-prototype/`. Lesson: sync docs PRZED `/10x-impl-review` (zamiast po) daje cleaner review signal — skill znalazł 3 realne issues w kodzie (timeout cleanup, plan contain↔cover swap, dead cards.ts) zamiast 20 ogromnych planning-vs-port driftów. F-01 pattern "docs must match shipped code before archive" staje się standard project workflow.
- **S-02: backend wystawia 3 REST endpointy (`POST /api/games`, `GET /api/games/{id}`, `POST /api/games/{id}/moves`) z view-model DTOs hiding opponent hand (anti-cheat), sync bot autoreply w tej samej response (driveBotMoves loop wywoływany z create + moves bo engine losuje startera ~50/50), inline legal moves jako structured objects z UPPER kind values (PASS/LEADER/UNIT/SPY/SPECIAL/ROW/UNIT_TARGET), per-game synchronized lock na wszystkich 3 endpointach (włącznie z getState — F1 critical fix z impl-review zapobiegający torn read podczas bot loop), in-memory `GameSessionStore` z 30-min cleanup, custom `GameNotFoundException` → 404 + `GameStateException` wrapper → 409 (żeby nie maskować Spring MVC's IllegalStateException), bot loop z safety counter (>200 iter → exception zamiast infinite loop), CORS dla localhost:5173 (+3000 jako defensive add). Frontend: Vite proxy `/api` → :8080, fetch wrappery w `src/api/gameApi.ts` z `parseError()` fallback do `res.statusText`, TypeScript types w `src/api/types.ts` mirror Java records, `/debug-game` route (text-only lists nazw + power numbers + collapsible JSON dump — twardy anti-drift żeby nie zostać miniaturką S-03). 238 testów green (8 nowych MockMvc integration tests, włącznie z `opponentHandHidden` anti-cheat check). S-01 routes (`/`, `/game/:stageId`) NIETKNIĘTE.** — Archived 2026-05-29 → `context/archive/2026-05-29-s-02-playable-game-api/`. Lesson: `/10x-plan-review` + `/10x-impl-review` każdy znalazł realne issues (7 fixów pre-code + 4 fixów post-code), włącznie z critical race condition w getState (torn read podczas bot loop) — workflow z lekcji broni przed shipnięciem bugów na produkcję. Plus: gdy używamy parallel Claude session do implementacji, główna sesja powinna sanity-checkować pliki po każdym phase (np. weryfikacja czy fixes faktycznie wylądowały w kodzie) — `/10x-impl-review` skill marked F5 jako "Skipped" mimo że było intencją do fixu, więc trust-but-verify.
- **S-03: piękne UI z S-01 dostaje życie — `CampaignMap` + `GameBoard.tsx` przepisane z mock state na `useReducer` + `GameContext` jako state machine (`idle → loading → selecting-target → waiting-for-bot → round-ended → match-ended`), click-to-select karta w hand → filter `legalMoves` po `handInstanceId` → highlight valid targets per move kind (UNIT na player rows, SPY na opponent rows, ROW dla horn/weather, UNIT_TARGET na own units dla decoy), pessimistic UI (wait for API response zgodnie z zasadą "frontend głupi/bezstanowy"), 600ms bot-thinking pause z "Bot is thinking..." indicator. Hand-built 28-entry `cardImageMap.ts` (F1 critical plan-review fix: backend English snake_case `"little_john"` ≠ frontend Polish kebab-case `"maly-john"` — zero overlap, bez tego wszystkie karty byłyby placeholderami). `Card.tsx` zaadaptowany do API shape (`cardId`/`currentStrength`/`basePower`) — F2 plan-review fix przesunięty z Phase 6 do Phase 2 żeby Phase 4 power color-coding miał na czym pracować. Visual feedback: weather row blue tint + ❄ icon, horn golden glow + 🎺 icon, power color-coding (red dla weather-weakened, green dla boosted by horn/morale/tight-bond, cream dla hero immunity). `RoundOverlay` (3s auto-dismiss banner) + `MatchEndScreen` (VICTORY/DEFEAT + R1/R2/R3 summary + Back to Campaign / Play Again). `CampaignMap` czyta `getHighestUnlocked()` z localStorage z try/catch defensive (F2 impl-review fix dla Safari private / quota / disabled storage). Component extraction w Phase 6: `BoardRow` (6×), `MetaPanel` (2×), `PlayerHand` — `GameBoard.tsx` 627 → 344 linii. `DebugGame.tsx` i `mockGameState.ts` usunięte. 5 impl-review fixes: `botTimerRef` cleanup na unmount (F1), localStorage try/catch (F2), `submittingRef` synchronous guard against double-submit (F3 — walidacja empirycznej obserwacji "2 karty zamiast 1"), `handleBack` timer cleanup (F4), MetaPanel `_leaderUsed` unused prop remove (F5). 72 modules build, zero TS errors.** — Archived 2026-05-29 → `context/archive/2026-05-29-s-03-polished-game-ui/`. Lesson: świadomy decyzyjny separator między baseline functionality a polish — wszystkie observed UX/gameplay nuances (opponent-passed indicator missing, weather/horn empirical validation, horn doubling bug 5+10+horn → 5 vanished, emoji vs SVG icons, match-end overlay uwagi) odłożone do osobnego dedicated polish change zamiast bugfix sprintu w środku north-star slice'a — trzyma scope ciasny i waliduje baseline produktu najpierw. Plus: `/10x-plan-review` znowu dowiódł wartości — F1 (card ID mapping) złapany przed kodem oszczędził shipowania frontend bez kart; reviewer poszedł i sprawdził pliki JSON żeby zweryfikować plan's assumption "IDs match".
